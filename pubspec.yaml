name: deudas_app
description: "Deudas, cobros y gastos: la app nativa contra la misma API."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  # Lo justo. Nada de librerias de estado gigantes ni de UI de terceros: la
  # app es una lista, un formulario y un grafico, y Material ya los trae.
  http: ^1.2.2                  # las llamadas a la API
  shared_preferences: ^2.3.2    # el token de sesion, como el localStorage
  provider: ^6.1.2              # el estado, sin ceremonia
  intl: ^0.19.0                 # fechas y montos en español de Nicaragua
  image_picker: ^1.1.2          # la cámara y la galería, para el comprobante
  # La API solo acepta el comprobante en JPEG (es el único formato que se
  # puede incrustar tal cual en el PDF del estado de cuenta). Una captura de
  # pantalla es PNG, y ni la cámara ni la galería garantizan el formato, así
  # que se recodifica aquí antes de subirlo.
  image: ^4.3.0
  # El estado de cuenta. La version web escribe el PDF a mano (byte a byte, con
  # su propia tabla de anchos de Helvetica) porque no podia traer dependencias;
  # aqui no hay esa limitante, y reescribir un generador de PDF a mano seria
  # trabajo de mas para un resultado peor.
  pdf: ^3.11.1
  printing: ^5.13.4           # compartir, guardar o imprimir el PDF
  share_plus: ^10.1.2         # el resumen en texto, para WhatsApp
  # Los recordatorios. Son notificaciones LOCALES, no push: las fechas de un
  # acuerdo de pago y de un mantenimiento se saben por adelantado, asi que no
  # hace falta que un servidor avise — la app las programa y el sistema las
  # muestra a su hora, sin internet.
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0           # para programarlas en la hora de Nicaragua

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
